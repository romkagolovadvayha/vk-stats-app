import React from 'react';
import './Contacts.scss';
import Layout from "../Layout/Layout";

const Contacts = () => {
    return (
        <Layout>
            Contacts page...
        </Layout>
    );
};

export default Contacts;
